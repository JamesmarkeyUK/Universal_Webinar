# Copy to .env.local for local development. Never commit .env files.
# See SUPABASE.md for how to get these values.

# Phase 2 — Supabase (required from Phase 2 onward)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...

# Phase 4 — LiveKit Cloud (URL only; API key/secret stay server-side in an Edge Function)
# VITE_LIVEKIT_URL=wss://YOUR-PROJECT.livekit.cloud
